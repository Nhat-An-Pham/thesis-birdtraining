import createTheme from "@mui/material/styles/createTheme";

export const ochreTheme = createTheme({
    palette: {
      ochre: {
        main: '#E3D026',
        light: '#E9DB5D',
        dark: '#A29415',
        contrastText: '#242105',
      },
    },
  });
export const groundTheme = createTheme({
    palette: {
      ground: {
        main: '#C8AE7D'
      }
    }
});